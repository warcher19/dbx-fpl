resources:
  pipelines:
    fpl_medallion:
      name: fpl_medallion

      catalog: fpl
      target: silver

      serverless: true
      channel: current

      libraries:
        - glob:
            include: ../src/bronze/**
        - glob:
            include: ../src/silver/**
        - glob:
            include: ../src/gold/**

      configuration:
        spark.databricks.delta.schema.autoMerge.enabled: "true"
